# Fish shell -- system-level only.
# User configuration (vi mode, aliases, shell integrations) is managed
# by Home Manager (home/modules/terminal/fish.nix).
{ ... }:

{
  programs.fish.enable = true;
}
